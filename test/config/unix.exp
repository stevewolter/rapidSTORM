proc program_start { cmd args } {
    global spawn_id
    global netcathost

    if { [info exists netcathost] } {
        spawn netcat $netcathost 1111
    } else {
        spawn $cmd $args
    }
}

initialize
